.gallery
	%ul{:class=>'elastislide-list', :id=>'carousel'}
		- i = 0
		- @user.images.each do |image|
			- i += 1
			%li
				%a{:href => '#', :class => 'portfolio_image_link', 'data-preview' => image.src.url(:original)}
					= image_tag image.src.url(:small), :alt => 'image' + i.to_s